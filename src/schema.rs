table! {
    products (id) {
        id -> Int4,
        title -> Varchar,
        description_short -> Varchar,
        description_long -> Varchar,
        price -> Float8,
        stock -> Int4,
        brand -> Varchar,
        color -> Varchar,
        size -> Varchar,
    }
}
